#!/usr/bin/env bash
# This script displays a list of processes containing the word "bash."

# Use 'ps' command to list all processes, and filter for lines containing "bash"
ps -eo pid,comm | grep -i "bash"
